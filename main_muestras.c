#include <stdio.h>
#include <stdlib.h>
#include "muestras.h"
int main(){

    Muestras *arrMuestras;
    int indice;
    arrMuestras=(Muestras *)malloc(N*sizeof(Muestras));
    llenarMuestras(arrMuestras);

 return 0;
}
